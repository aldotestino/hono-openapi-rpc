{
  "name": "hono-openapi-rpc",
  "type": "module",
  "private": true,
  "scripts": {
    "api": "bun run --cwd apps/api dev",
    "web": "bun run --cwd apps/web dev"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}